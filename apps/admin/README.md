图标用法（仅 Lucide）

1. 打开菜单「图标」字段，在 IconPicker 输入关键字（如 `user`、`set`）模糊搜索
2. 悬浮窗分页列表点选，或直接输入完整短名回车确认
3. 也可到 https://icon-sets.iconify.design/lucide/ 查阅后粘贴短名（不要带 `lucide:` 前缀）
4. 代码里：`<Icon icon="user" />`；静态装饰也可用 `class="i-lucide-user"`
